% ===================================================================================
% NARRENSICHERE LEBENSLAUF-VORLAGE
% Verwendet nur Standard-LaTeX, keine externen Schriftarten oder Pakete.
% Kompiliert mit pdflatex.
% ===================================================================================
\documentclass[a4paper,11pt]{article}

% --- Minimale, standardmäßige Pakete ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[ngerman]{babel}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{hyperref}

% --- Seitenlayout ---
\geometry{left=2cm, right=2cm, top=2cm, bottom=2cm}
\pagestyle{empty}
\setlength{\parindent}{0pt}

% --- Platzhalter-Befehl ---
\newcommand{\VAR}[1]{\texttt{#1}}

% --- Dokumentenbeginn ---
\begin{document}

% --- KOPF ---
    \begin{center}
    {\Huge \bfseries \VAR{name}}\\[5pt]
    {\large \VAR{titel}}\\[10pt]
    \VAR{adresse} $\cdot$ \VAR{email} $\cdot$ \VAR{telefon}
    \end{center}
    \vspace{10mm}

% --- BILD (optional, auskommentieren, wenn nicht benötigt) ---
% \begin{flushright}
%     \includegraphics[width=3cm]{\VAR{photo_path}}
% \end{flushright}
% \vspace{-40mm} % Negative space to pull text up

% --- ÜBER MICH ---
    \section*{Über mich}
    \rule{\textwidth}{0.4pt}
    \vspace{2mm}
    \VAR{ueber_mich_text}
    \vspace{8mm}

% --- BERUFSERFAHRUNG ---
    \section*{Berufserfahrung}
    \rule{\textwidth}{0.4pt}
    \VAR{berufserfahrung_block}

% --- PROJEKTERFAHRUNG ---
    \section*{Projekterfahrung}
    \rule{\textwidth}{0.4pt}
    \VAR{projekterfahrung_block}

% --- AUSBILDUNG ---
    \section*{Ausbildung}
    \rule{\textwidth}{0.4pt}
    \VAR{ausbildung_block}

% --- KENNTNISSE ---
    \section*{Kenntnisse}
    \rule{\textwidth}{0.4pt}
    \vspace{2mm}
    \VAR{kenntnisse_block_simple}

\end{document}